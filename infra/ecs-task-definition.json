[
  {
    "family": "flowsentinel-python-engine",
    "networkMode": "awsvpc",
    "requiresCompatibilities": ["FARGATE"],
    "cpu": "512",
    "memory": "1024",
    "executionRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/ecsTaskExecutionRole",
    "taskRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/flowsentinelTaskRole",
    "containerDefinitions": [
      {
        "name": "flowsentinel-python-engine",
        "image": "<ECR_REPO>/flowsentinel-python-engine:<TAG>",
        "essential": true,
        "portMappings": [{ "containerPort": 8002, "protocol": "tcp" }],
        "environment": [
          { "name": "LISTEN_PORT", "value": "8002" },
          { "name": "REDIS_ADDR", "value": "<REDIS_ENDPOINT>:6379" },
          { "name": "REDIS_DB", "value": "0" },
          { "name": "RULES_REFRESH_SECONDS", "value": "30" }
        ],
        "secrets": [
          { "name": "POSTGRES_DSN", "valueFrom": "<SSM_OR_SECRETS_MANAGER_ARN_FOR_POSTGRES_DSN>" },
          { "name": "REDIS_PASSWORD", "valueFrom": "<SSM_OR_SECRETS_MANAGER_ARN_FOR_REDIS_PASSWORD>" }
        ],
        "logConfiguration": {
          "logDriver": "awslogs",
          "options": {
            "awslogs-group": "/ecs/flowsentinel-python-engine",
            "awslogs-region": "<REGION>",
            "awslogs-stream-prefix": "ecs"
          }
        },
        "healthCheck": {
          "command": ["CMD-SHELL", "wget -qO- http://localhost:8002/health || exit 1"],
          "interval": 30,
          "timeout": 5,
          "retries": 3,
          "startPeriod": 20
        }
      }
    ]
  },
  {
    "family": "flowsentinel-admin",
    "networkMode": "awsvpc",
    "requiresCompatibilities": ["FARGATE"],
    "cpu": "256",
    "memory": "512",
    "executionRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/ecsTaskExecutionRole",
    "taskRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/flowsentinelTaskRole",
    "containerDefinitions": [
      {
        "name": "flowsentinel-admin",
        "image": "<ECR_REPO>/flowsentinel-admin:<TAG>",
        "essential": true,
        "portMappings": [{ "containerPort": 8001, "protocol": "tcp" }],
        "secrets": [
          { "name": "DATABASE_URL", "valueFrom": "<SSM_OR_SECRETS_MANAGER_ARN_FOR_DATABASE_URL>" }
        ],
        "logConfiguration": {
          "logDriver": "awslogs",
          "options": {
            "awslogs-group": "/ecs/flowsentinel-admin",
            "awslogs-region": "<REGION>",
            "awslogs-stream-prefix": "ecs"
          }
        },
        "healthCheck": {
          "command": ["CMD-SHELL", "wget -qO- http://localhost:8001/health || exit 1"],
          "interval": 30,
          "timeout": 5,
          "retries": 3,
          "startPeriod": 10
        }
      }
    ]
  }
]
